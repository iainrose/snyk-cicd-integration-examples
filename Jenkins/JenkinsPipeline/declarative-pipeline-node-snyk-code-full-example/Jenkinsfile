pipeline {
    agent any

    // Install the Jenkins tools you need for your project / environment
    tools {
        snyk 'Snyk' // Refers to a global tool configuration for Snyk called 'Snyk'
    }

    stages {
        
        stage('Run Snyk Scan') {
            steps {
                snykSecurity additionalArguments: '--code', monitorProjectOnBuild: false, snykInstallation: 'Snyk', snykTokenId: 'snyk_api_token'
            }
        }
    }
}
